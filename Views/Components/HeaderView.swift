import SwiftUI

struct HeaderView: View {
    var body: some View {
        Text("DM's Toolkit")
            .font(.largeTitle)
            .foregroundColor(Color(hex: "64b5f6"))
            .padding(.vertical, 16)
    }
}
